# detox research validation — adversarial fact-check

Method: fresh re-verification in `/private/tmp/claude-502/-Users-kerry-hatcher-projects/144be503-4a3a-4fb6-8eda-b4b656539f0b/scratchpad/validate-01/` —
own `brew`/binary runs, own `café.txt`/collision/symlink/dotfile/wipeup/max_length repros, own
`config_file.c`/`sequence.c`/`parse_options.c`/`clean_string.c`/`file.c` reads from a freshly
downloaded `detox-main.tar.gz`, and own GitHub REST API calls (9 requests used of 60/hr budget
before this session, 51 remaining after).

## Claim verification table

| Claim | Source | Verdict | Evidence |
|---|---|---|---|
| Version-conflation premise ("doc 01 claims installed detox is 1.4.x somewhere") | Task premise vs. doc 01 | **REFUTED** | `grep -n "1\.4" doc01` → zero hits. Doc 01 states 3.0.1 consistently throughout (title, §1, §2.1, §4, §8). `brew list --versions detox` → `3.0.1`; `detox -V` → `detox 3.0.1`; Cellar path `/opt/homebrew/Cellar/detox/3.0.1`. No version conflation exists in the doc as written. |
| Installed version is 3.0.1 | Doc 01 throughout | CONFIRMED | `brew info detox`, `detox -V`, Cellar path all agree: 3.0.1. |
| `safe.tbl` has no `default` line; maps C0+DEL+space+`!"$'*/:;<>?@\`\|` → `_`, `()[]{}`→`-`, `&`→`_and_`; bytes ≥0x80 pass through | Doc 01 §4 | CONFIRMED | Read `/opt/homebrew/share/detox/safe.tbl` verbatim — matches exactly. |
| A "legacy safe/unicode table set" ships at `.../legacy/*.tbl` with `é`→`e`, `ß`→`ss`, `Þ`→`TH`, currency codes, `default _` | Doc 01 §4 | **PARTIALLY REFUTED** | `ls .../legacy/` → `cp1252.tbl iso8859_1.tbl unicode.tbl unidecode.tbl`. **There is no legacy `safe.tbl`.** The transliteration examples (é→e at 0x00E9, ß→ss at 0x00DF, Þ→TH at 0x00DE, `default _`) are all real and confirmed, but they live only in legacy `unicode.tbl`, not in a "legacy safe" table — doc 01's phrase "legacy safe/unicode table set" implies a legacy `safe.tbl` exists; it does not. |
| CLI flag table (`-f`,`-h`,`-L`,`-n`,`-r`,`-s`,`--special`,`-v`,`-V`,`--inline`) | Doc 01 §1 | CONFIRMED (spot-checked `-n`,`-r`,`-s`,`--special`,`-V`, `DETOX_SEQUENCE`) | Reproduced live. |
| Config search order: SYSCONFDIR → /etc → /usr/local/etc → merge $HOME/.detoxrc → merge $XDG_CONFIG_HOME/detox/detoxrc | Doc 01 §2.1 | CONFIRMED | Read `src/config_file.c:38-97` directly (fresh download) — exact order, exact merge-on-top semantics (`parse_config_file(path, config_file, ...)` passes prior result in as base for HOME/XDG steps). No user/XDG detoxrc exists on this machine so the *merge itself* wasn't behaviorally reproduced, only source-verified. |
| `-s` CLI flag overrides `DETOX_SEQUENCE`; fallback to `default` sequence name, then first-defined sequence | Doc 01 §1, §2.2 | CONFIRMED | `src/parse_options.c` sets `sequence_name = getenv("DETOX_SEQUENCE")` at init, then `case 's'` overwrites it. `src/sequence.c: sequence_choose_default()` matches doc exactly, including "only falls back to first sequence when `name==NULL`" (i.e., only when *no* `-s`/env value at all, not when the named sequence is merely missing). |
| `café.txt` → `cafÃ©.txt` under `-s iso8859_1` | Doc 01 §7-8 | CONFIRMED | Reproduced verbatim: `touch café.txt && detox -n -v -s iso8859_1 café.txt` → `café.txt -> cafÃ©.txt`. |
| Collision refusal: same-inode/single-hardlink exception; refusal message `Cannot rename <old> to <new>: file already exists` to stderr | Doc 01 §5 | CONFIRMED | Reproduced exact stderr text; independently confirmed against `src/file.c:124-153` (`st_dev`/`st_ino`/`st_nlink>1` logic matches exactly). |
| `-n`/dry-run still runs collision check and prints the error unconditionally (not gated on `-v`) to stderr | Doc 01 §5, §8.2 | CONFIRMED | Reproduced with stdout/stderr captured separately: error appears on stderr under plain `-n` (no `-v`), stdout empty. |
| `--special` gate: without it, a symlink named directly on the CLI is silently skipped (only "Scanning:" printed); with it, renamed | Doc 01 §6, §8.3 | CONFIRMED | Reproduced exactly both ways. |
| Dotfiles skipped only inside `-r` recursion, not when named explicitly or at top level | Doc 01 §6, §8.9 | CONFIRMED | Reproduced: `-r .` skips `.hidden file.txt`; explicit `detox -n -v ".hidden file.txt"` renames it. |
| `wipeup` leading-strip of `#`/`-`/`_`; run-collapse precedence `.` > `-` > `_` (positional, via `strchr`) | Doc 01 §2.3, §8.6-7 | CONFIRMED | Reproduced `this__--.txt` → `this.txt` under `remove_trailing`. Independently confirmed against `src/clean_string.c: clean_wipeup()` — leading-strip loop is literally `*filename=='-'||'_'||'#'`; collapse uses `strchr(search,...)` with `search` = `.-_` or `-_`, comparing pointer position (`seek < current`) — exactly the mechanism doc 01 describes. |
| `max_length`: default 256, extension-aware, ≤5-char backward scan for a second dot (preserves `.tar.gz`), gives up unchanged if extension ≥ N bytes | Doc 01 §2.3, §8.8 | CONFIRMED | Reproduced both example truncations exactly (`this_is_my_file_.txt`, `this_is_my_ar.tar.gz` at length 20). Independently confirmed against `src/clean_string.c: clean_max_length()` — the `while (--input_walk > filename) { if (extension-input_walk>5) break; ... }` loop matches doc's "5-character lookback" description exactly, including the single-char-extension short-circuit (`strlen(extension)==1`). |
| Repo stats: archived=true, open_issues=0, 446 stars, 24 forks, pushed 2026-07-12 | Doc 02 header | CONFIRMED | Own `curl .../repos/dharple/detox` call, same day, identical values. |
| Total issues+PRs = 140, all closed, 0 open | Doc 02 header | CONFIRMED | Own paginated fetch (`state=all&per_page=100`, 3 pages) → 100+40+0 = 140, all closed. |
| 13 PRs total; 8 merged (#18,#88,#92,#93,#97,#101,#107,#113), 5 unmerged (#15,#58,#130,#133,#136) | Doc 02 table + PR list | CONFIRMED | Own list of `pull_request` items = exactly these 13 numbers; own `.../pulls/{n}` merged-field checks for all 13 confirm the 8/5 split exactly as claimed. |
| #130 title, state, unmerged, and technical rejection content (N-files-collapse risk, `readdir()` ordering, `S_ISREG`/`S_ISDIR` checks, BSD/macOS syscall differences, "I don't want to be responsible for destroying other people's data") | Doc 02 table, theme 6 | CONFIRMED | Own fetch of issue + `pulls/130` (merged=false) + `/issues/130/comments` — quote is verbatim; all four technical points are verbatim/faithfully paraphrased from the maintainer's actual comment. Doc's characterization is fair, not embellished. |
| #124 title, ask, workaround, and roadmap quotes ("I have had many requests of this nature"; wrapper-layer-or-rewrite quote) | Doc 02 table, theme 1, "Maintainer's Stated Future Direction" | CONFIRMED | Own fetch of issue + comments. Title matches exactly. "I have had many requests of this nature" is verbatim from the final (2026-07-12) comment. The wrapper-layer/rewrite quote is a faithful synthesis of two adjacent comments by the same maintainer (2025-08-11), not a single verbatim block as doc 02's formatting might imply — a minor fidelity nit, not a fabrication. |
| Maintainer quotes elsewhere ("putting detox on hold", "I don't plan on working any further...") | Doc 02, multiple rows | CONFIRMED (spot-checked #124's final comment) | Exact match: "At this time, I don't plan on working on this. I'm putting detox on hold, because I don't have enough time to give detox and its users the attention they deserve." |
| "Symlink handling: only tangential... no issue directly reports a symlink bug or requests symlink-following behavior" | Doc 02, "Themes checked but with weak or no evidence" | **REFUTED** | Issue **#23** "Fix relative link recursion or remove support for it" is a direct, first-person symlink bug report: a user ran `detox -r --special` across a symlink pointing at `../..` in `/tmp` and it recursed across their entire dev-projects directory by accident — a real, reproducible symlink-recursion incident, not a hypothetical. Issue **#20** "Add tests for --special" explicitly calls out symlink loops and `.`/`..`-referencing symlinks as an untested gap. Doc 02's absence claim is wrong; this should have been its own theme, not a "weak/no evidence" footnote. |
| "Performance on large trees: only one adjacent data point (#96)... no issue asks for faster large-tree processing" | Doc 02, "Themes checked but with weak or no evidence" | CONFIRMED as stated, but incomplete | No throughput/speed complaint found either (own keyword search across all 140 titles+truncated bodies for "slow/performance/throughput/benchmark" found nothing). However, doc 02 undercounts *crash/memory-safety* bugs as a class: besides #96 (double free, 2TB run), **#137** ("malloc(): invalid size (unsorted) with certain input when deleting characters") and **#11** ("crash on directory with carriage returns and spaces") are two more distinct, reproducible crash reports not mentioned anywhere in doc 02. The "only large-scale stability bug found" framing for #96 is misleading — there are at least 3 independent crash bugs in the tracker, not 1. |
| No CJK-specific issue found by name | Doc 02, theme 9 + confidence notes | CONFIRMED | Own keyword search (chinese/japanese/korean/cjk/wide char) across all 140 titles+bodies found nothing. Doc 02's hedge here ("inferred adjacency, not directly verified") is accurate and appropriately cautious. |
| No inode/hardlink issue found | Doc 02, "weak or no evidence" | CONFIRMED | Own keyword search found none; consistent with doc 02. |

## Corrections Required

1. **Doc 01 §4**: Change "A legacy safe/unicode table set... ships separately at `.../legacy/*.tbl`" to something like "A legacy transliteration table set (`unicode.tbl`, `iso8859_1.tbl`, `cp1252.tbl`, `unidecode.tbl` — **no legacy `safe.tbl` exists**) ships at `.../legacy/*.tbl`." The current wording implies a legacy `safe.tbl` with aggressive transliteration exists; it doesn't — the transliteration lives in legacy `unicode.tbl` only.
2. **Doc 02, Theme Synthesis / "Themes checked but with weak or no evidence"**: Remove or rewrite the "Symlink handling" dismissal. Issue #23 is a direct, first-person symlink-recursion bug (accidental blast-radius traversal via a relative symlink under `-r --special`), and #20 explicitly flags symlink-loop test gaps. This should become its own themed section (or fold into "Cross-platform portability" / a new "Symlink safety" theme) with **hard-requirement** framing for a rewrite: relative-symlink recursion under `--special -r` is an unbounded-blast-radius footgun, confirmed by a real user incident, not a theoretical concern.
3. **Doc 02, theme "Performance on large trees" / #96 framing**: Correct "the only large-scale stability bug found" to acknowledge #137 (malloc invalid-size crash on character-deletion input) and #11 (crash on directory names containing carriage returns) as two additional, independent memory-safety/crash bugs. Reframe as "at least 3 independent crash/memory-safety bugs (#11, #96, #137), none clearly resolved in visible threads" rather than a single isolated data point.
4. **Doc 02, #124 quote presentation**: The roadmap quote block reads as one continuous verbatim statement but is actually a synthesis of two separate comments by the same maintainer, 37 minutes apart on 2025-08-11. Not a fabrication, but the doc should mark the synthesis (e.g. "...combining two comments in the same thread...") to avoid appearing as a single verbatim quote.
5. The task's stated "priority target #1" (doc 01 conflating installed version 1.4.x with v3.0 design) **does not exist in the document as written** — flag this back to whoever set that priority; it may have been based on a stale/different draft than the one at the given path.

## Load-Bearing Uncertainties (do not assume in the Rust design)

- **Config merge behavior is source-verified only, not behaviorally verified** on this machine — no `$HOME/.detoxrc` or `$XDG_CONFIG_HOME/detox/detoxrc` exists here, so the actual "user sequence with same name replaces system sequence" merge was never exercised end-to-end, only read in `config_file.c`. Do not assume subtle merge edge cases (e.g., partial-parse-failure fallback ordering) beyond what the ~60-line function shows.
- **Hard-link collision branch (`st_nlink > 1`)** — confirmed in source, never behaviorally reproduced (by either agent). Treat as source-verified only.
- **Comments were not fetched for ~116 of 140 issues** (by either this pass or the prior one) — absence claims (no CJK request, no inode/hardlink issue, "only" N crash bugs) are bounded by title+body keyword search, not full comment-thread reading. A comment-only mention could still exist. Symlink handling itself flipped from "absent" to "present" purely by finding two more title matches — the same could happen for other "absent" themes with more thorough searching.
- **Whether shipped on-disk `.tbl` files are byte-identical to compiled-in builtins** — inferred from the existence of `bin/generate-builtin.sh`, never diffed at the byte level by anyone. Don't assume builtin-vs-file parity is guaranteed at runtime for every distro packaging.
- **Genuinely mis-encoded (non-UTF-8) Latin-1 filenames** were never materialized or tested against `iso8859_1` on this filesystem (APFS rejects invalid UTF-8 names at the syscall level) — the reverse corruption (valid UTF-8 fed to `iso8859_1`) was reproduced instead. The forward case (mis-encoded name → corrected) remains unverified behaviorally on any platform in this research.
- **Rate-limit budget is a hard constraint for further verification**: 51/60 unauthenticated requests remained after this pass; only 24 of 140 issues have ever had comments fetched (by prior + this agent combined). Any further "maintainer said X" claims for issues outside that set are unverified until fetched.
