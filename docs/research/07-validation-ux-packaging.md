# Adversarial Fact-Check of 04-ux-config-and-distribution.md

> **Stage-3 revision, 2026-07-31.** This document was itself adversarially reviewed (reviewers
> L1/L2/L3). No fabrication was found and every version, date, and URL L1 re-checked resolved
> exactly. One row (**8b**) credited a primary source whose fetch had actually failed; it has been
> corrected and, better, re-grounded in real primary sources. See the
> [Review record (stage 3)](#review-record-stage-3) at the end. Changes are marked `[stage-3]`.

## Evidence precedence (where this document sits in the hierarchy)

Doc 00 treats docs 05/06/07 as the top of the evidence hierarchy, and doc 01 defers to docs
10-13. Neither rule said what happens between doc 05 and docs 10-13. The full rule, stated here
so a reader cannot pick the wrong authority:

1. **Docs 10-13 win** on any claim about the upstream C tool's source or behavior, over both doc
   01 and doc 05. They are newer, source-derived, and independently validated.
2. **Doc 05 wins over doc 01** on C-tool claims that docs 10-13 do not cover.
3. **This document (07) and doc 06 win over docs 04 and 03 unconditionally.** Docs 10-13 have
   **no jurisdiction here**: this document is about external tools, crates, packaging, and UX
   convention, not about detox's own source, so there is no newer authority to defer to. Where
   this document and doc 04 disagree, this document wins.

One row is the exception that proves the rule useful: **row 8c** is a claim about the C tool
(does detox default to dry-run?), so tier 1 applies to it and docs 10-13 outrank this document
there. They agree — doc 10 derives the same answer from source — so nothing turns on it. Beyond
that, a row marked `CORRECTED` below is not authority for its original claim; read the row, not
the tier.

## Verdict vocabulary and evidence labels

Verdicts (used consistently across docs 05, 06, 07):

| Verdict        | Meaning                                                                     |
| -------------- | --------------------------------------------------------------------------- |
| `CONFIRMED`    | Claim verified true as written, independently, in this document's own pass. |
| `REFINED`      | Claim true in substance, but needs a caveat, correction, or narrowing.      |
| `REFUTED`      | Claim false, or backwards.                                                  |
| `CORRECTED`    | A verdict **this document previously got wrong**, fixed at stage 3.         |
| `UNVERIFIED`   | Not checked in any pass. No opinion offered.                                |
| `UNVERIFIABLE` | Cannot be checked as written; reason stated inline.                         |

`CONFIRMED` now means independently verified **here**. Where a row upholds doc 04's own citation
without re-fetching it, the verdict says `CARRIED FORWARD` instead — rows 1f, 8a. Reviewer L3 was
right that using the same word for both overstates confidence.

Every evidence cell carries a strength label, because doc 00 weights these very differently:

- `[MEASURED]` — measured on this hardware, in this session (local binary, local man page).
- `[SOURCE]` — read from source code or a spec. Not behavioral.
- `[WEB]` — fetched from a live API or official documentation page.
- `[SECONDARY]` — from a blog, Q&A thread, or search summary; primary fetch failed or absent.
- `[INHERITED]` — carried over from doc 04's own citation without re-verification.

## Citation index (stable identifiers)

Rows keep their original `1a`-`9b` identifiers, which doc 00 already cites unambiguously and
which this pass does not renumber. Corrections are `C1`-`C6` in their original order, and
Load-Bearing Uncertainties are numbered `U1`-`U5`. **All identifiers are stable — append, never
renumber.** Doc 00 cites this document 20 times and every citation resolves; the only one whose
_content_ changed is **row 8b**, whose Fedora/Debian/Arch/nixpkgs list is now primary-sourced
rather than search-summarized (doc 00 lines 1424 and 1755 restate it without its original hedge,
which is no longer a problem for the distro list itself — see C6).

Verification pass against primary sources (official docs, source code, crates.io/GitHub APIs, local
man pages/binaries). Default posture: unverified unless independently confirmed here.

## Claim / Verdict / Evidence

| #   | Claim (from 04-*.md)                                                                                                                                         | Verdict                                                                                                                                                                                                                                                     | Evidence                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| --- | ------------------------------------------------------------------------------------------------------------------------------------------------------------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1a  | f2 defaults to dry-run; `-x`/`--exec` writes changes                                                                                                         | CONFIRMED                                                                                                                                                                                                                                                   | `[WEB]` f2 docs; confirmed current latest release is **v2.2.2** (2025-11-10), not stated in doc. https://f2.freshman.tech/reference/cli-options, https://github.com/ayoisaiah/f2/releases                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| 1b  | f2's undo journal format/location — doc's own flagged unverified item                                                                                        | **RESOLVED — REFINED**                                                                                                                                                                                                                                      | `[SOURCE]` Read f2 source directly (`rename/backup.go`, `internal/config/config.go`, tag v2.2.2 tree). Journal is **JSON**, filename = `md5(workingDir) + ".json"`, stored at **`os.TempDir()/f2/backups/<hash>.json`** — i.e. the OS **temp** directory, not XDG state/data/config. This matters: temp dirs are commonly cleared on reboot/tmpwatch, so f2's "undo" is not durable across a restart on many systems. Any design doc treating f2 as precedent for "put the undo journal in XDG_STATE_HOME" would be wrong — f2 itself doesn't do that. Source: https://github.com/ayoisaiah/f2/blob/master/rename/backup.go                                                                                                                                                                                                                                                                                                                                                                                                                               |
| 1c  | rnr defaults to dry-run; `-f`/`--force` writes                                                                                                               | CONFIRMED                                                                                                                                                                                                                                                   | `[WEB]` README states "Dry-run by default"; `-f`/`--force` executes. Latest tag **v0.5.1** (2025-12-13), repo not archived, last push 2026-03-21 — actively maintained. https://github.com/ismaelgv/rnr                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| 1d  | sd offers opt-in `-p`/`--preview`, no dry-run by default                                                                                                     | CONFIRMED                                                                                                                                                                                                                                                   | `[WEB]` README: preview requires explicit `-p`. Default is in-place write. Latest tag v1.1.0 (2026-02-25). **Correction to how the doc frames this**: sd is a sed-replacement (text find/replace inside files), not a file-renaming tool — the doc already labels it correctly as "(Rust sed-alternative)" so no mischaracterization here, but flag that sd is not part of the "rename tool" comparison set and its inclusion as a contrast case (not a peer) should stay explicit. https://github.com/chmln/sd                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| 1e  | rclone sync executes destructively without `--dry-run`; ships guidance to test first                                                                         | CONFIRMED                                                                                                                                                                                                                                                   | `[WEB]` Official docs: "Since this can cause data loss, test first with the `--dry-run` or the `--interactive`/`-i` flag." rclone is a sync/copy tool, not a renamer — doc correctly frames it as a contrast, not a peer. https://rclone.org/commands/rclone_sync/                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| 1f  | git clean refuses without `-n`/`-f`/`-i`, enforced by `clean.requireForce`                                                                                   | **CARRIED FORWARD** `[stage-3]` (was "CONFIRMED (per general docs; not re-verified against git source in this pass)")                                                                                                                                       | `[INHERITED]` Consistent with Atlassian/git docs cited by the doc ([`git clean` man page](https://git-scm.com/docs/git-clean), [`clean.requireForce`](https://git-scm.com/docs/git-config#Documentation/git-config.txt-cleanrequireForce) — linked here at stage 3, still not re-read). Not independently re-checked against git's own source this round — treat as medium confidence.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| 1g  | ripgrep and fd have no dry-run-style behavior claimed                                                                                                        | N/A — correctly excluded                                                                                                                                                                                                                                    | `[SOURCE]` The doc does **not** attribute dry-run/rename behavior to ripgrep or fd; they only appear in Section 4 as directory-traversal tooling (`ignore` crate). No mischaracterization found.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| 2a  | ripgrep: config is opt-in only via `RIPGREP_CONFIG_PATH`, no auto-discovery                                                                                  | CONFIRMED                                                                                                                                                                                                                                                   | `[MEASURED]` + `[WEB]` `man rg` (local, installed rg) and GUIDE.md agree: rg "will not look in any predetermined directory... you need to set `RIPGREP_CONFIG_PATH`."                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| 2b  | ripgrep config file is "a flattened CLI-argument list"; comments via `#`, empty lines OK, multi-line flag+value works                                        | CONFIRMED, with precision added                                                                                                                                                                                                                             | `[WEB]` GUIDE.md's own two rules: "(1) every line is a shell argument, after trimming whitespace; (2) lines starting with `#` (optional leading whitespace) are ignored." Empty lines explicitly OK. No escaping — each line is one literal argv entry verbatim. A flag and its value can be on the same line (`--max-columns=150`) or two separate lines; both are equivalent. The doc's "flattened CLI-argument list" phrasing is not GUIDE.md's exact wording but is an accurate paraphrase.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| 3   | ruff: upward directory walk, priority `.ruff.toml` > `ruff.toml` > `pyproject.toml`, first-match-wins, **no cross-level merge except via explicit `extend`** | CONFIRMED — doc already correctly caveats this                                                                                                                                                                                                              | `[WEB]` Astral docs confirm no merge across directory levels ("closest configuration file is used, and any parent configuration files are ignored") and that `extend` is the explicit, opt-in mechanism to inherit a parent config. The doc's own text already states this exact nuance ("...unless the user explicitly opts in via an `extend` field"). **This claim, as written, is not wrong** — the instruction's suspicion that the doc mischaracterized ruff does not hold up; the doc got the `extend` caveat right the first time. https://docs.astral.sh/ruff/configuration/                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| 3b  | Prettier: upward walk, first found wins, no merge across levels, no global config                                                                            | CONFIRMED                                                                                                                                                                                                                                                   | `[WEB]` Prettier docs: config resolved by searching up from the target file; "Prettier intentionally doesn't support any kind of global configuration... behavior stays the same [across machines]." No merge language found; nearest file fully governs. https://prettier.io/docs/configuration                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| 4   | XDG_CONFIG_HOME → `$HOME/.config`, XDG_STATE_HOME → `$HOME/.local/state`, etc.; undo journal belongs in STATE                                                | CONFIRMED, verbatim                                                                                                                                                                                                                                         | `[WEB]` Spec: STATE "contains state data that should persist between (application) restarts... may contain: actions history (logs, history, recently used files, …)." Matches doc's table exactly. https://specifications.freedesktop.org/basedir/latest/                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| 5   | NO_COLOR: any non-empty value suppresses color; CLICOLOR/CLICOLOR_FORCE older/competing; NO_COLOR > CLICOLOR_FORCE > CLICOLOR when both implemented          | CONFIRMED, not conflated                                                                                                                                                                                                                                    | `[WEB]` + `[SECONDARY]` no-color.org: "present and not an empty string (regardless of its value)." bixense spec text (as summarized by fetch): NO_COLOR takes absolute priority; CLICOLOR_FORCE next; CLICOLOR lowest; FORCE_COLOR is treated as an alias for CLICOLOR_FORCE. The doc keeps these as three distinct signals and states the correct precedence order — no conflation found.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| 6   | Terraform apply refuses a saved plan if state drifted underneath it                                                                                          | CONFIRMED (via community/GitHub evidence, not the current official apply/plan pages)                                                                                                                                                                        | `[WEB]` Official `apply`/`plan` pages do **not** state this explicitly (checked directly — no drift/staleness language on either page). The actual mechanism — Terraform's real, well-documented "Error: Saved plan is stale" — is confirmed via HashiCorp's own GitHub issue tracker and third-party writeups: "The given plan file can no longer be applied because the state was changed by another operation after the plan was created." **Correction to the doc's citation**: cite the actual stale-plan behavior/error, not the apply/plan doc pages, which don't spell this out. https://github.com/hashicorp/terraform/issues/21785, https://github.com/hashicorp/terraform/issues/25981                                                                                                                                                                                                                                                                                                                                                         |
| 7a  | clap, ignore, assert_cmd, trycmd, insta, rayon, proptest, criterion, clap_complete, clap_mangen — "actively maintained"                                      | CONFIRMED for all except figment (see below)                                                                                                                                                                                                                | `[WEB]` crates.io API, latest stable + last-updated: clap 4.6.5 (2026-07-31), clap_complete 4.6.8 (2026-07-27), clap_mangen 0.3.0 (2026-03-27), ignore 0.4.31 (2026-07-20), config 0.15.25 (2026-06-26), assert_cmd 2.2.2 (2026-05-11), trycmd 1.2.1 (2026-07-21), insta 1.48.0 (2026-06-11), rayon 1.12.0 (2026-04-14), proptest 1.11.0 (2026-03-24), criterion 0.8.2 (2026-02-04). All updated within the last ~6 months of "today" (2026-07-31).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| 7b  | figment — "actively maintained... widely used"                                                                                                               | **REFUTED/REFINED — stale**                                                                                                                                                                                                                                 | `[WEB]` crates.io: figment's last published update is **2024-05-17**, over two years before today. A community fork (`figment2`, v0.11) exists, which is itself often a signal the original stopped receiving attention. The doc should not present figment as equivalently "actively maintained" alongside `config-rs`; flag it as a staleness risk for a new project's dependency choice. https://crates.io/crates/figment, search results referencing `figment2` as a "remaintained fork"                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| 7c  | cargo-dist actively maintained, current                                                                                                                      | **REFINED** `[stage-3]` (was "CONFIRMED but incomplete") — the crate is active and the rename is real, but the rename itself rests on secondary sources: the primary announcement was unreachable                                                           | `[WEB]` + `[SECONDARY]` crates.io: `cargo-dist` latest 0.32.0 (2026-05-22) — active. **But**: axo.dev renamed the _tool_ from "cargo-dist" to **"dist"** in Oct 2024 ("say hi to dist, or, cargo-dist no more!" — could not fetch blog.axo.dev directly, DNS failure in this session, but corroborated via search-result summary and GitHub/docs references to the "dist" book/install page). The crates.io package name **"dist" is a dead placeholder crate, version 0.0.0, published 2016** — unrelated, pre-existing squat — so axo's own rename to a bare `dist` crate name on crates.io is blocked/incomplete; `cargo-dist` remains the installable crate name in practice. The doc should say "cargo-dist, now branded `dist`" rather than treating the name as static — a reader following doc links to "cargo-dist on crates.io" is following the old brand. axo.dev itself (fetched directly) shows no sign of the company shutting down as of this check. https://crates.io/crates/cargo-dist, https://crates.io/crates/dist, https://axo.dev/ |
| 7d  | release-plz actively maintained                                                                                                                              | CONFIRMED                                                                                                                                                                                                                                                   | `[WEB]` crates.io: release-plz latest 0.3.160, updated 2026-07-14 — very actively maintained (frequent point releases).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| 8a  | Debian requires all deps packaged via debcargo, no network at build, crate-per-source-package                                                                | **CARRIED FORWARD** `[stage-3]` (was "CONFIRMED (doc's own citation stands)" — reworded because nothing was re-verified here)                                                                                                                               | `[INHERITED]` Not independently re-fetched this round beyond what's in the doc; the Debian Rust Team's own [packaging book](https://rust-team.pages.debian.net/) and [Debian wiki Rust page](https://wiki.debian.org/Rust) are primary sources (URLs added at stage 3; still not re-read) and the claim is consistent with well-known Debian Rust packaging practice. Medium-high confidence, not re-verified live.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| 8b  | detox "packaged in all major distros" — Debian, Ubuntu, Mint, Arch confirmed; Fedora "referenced in passing... not independently confirmed"                  | **REFINED, and `[stage-3]` CORRECTED as to sourcing.** Fedora confirmed — now from a primary source, not Repology. A real risk the doc missed is also surfaced. **This row originally credited Repology as the source when that fetch had in fact failed.** | `[SECONDARY]` → `[WEB]` `[stage-3]` **Sourcing corrected, then improved.** The original evidence read "Repology: detox is packaged in Debian 11-14/unstable, Fedora 38-44 + Rawhide, Arch, and nixpkgs 23.11-26.05/unstable" and credited [Repology](https://repology.org/project/detox/packages) as a primary source. It was not: the direct fetch failed and the list came from a WebSearch summary, as this document's own U1 admitted while the row above it did not. **Repology has now failed a fourth time** (`curl https://repology.org/api/v1/project/detox` → connection timed out after 45 s, HTTP 000, this session), so it should be treated as unreachable from this environment rather than retried a fifth time. Instead the claim was re-grounded in each distribution's **own** API, which is strictly better than Repology (a secondary aggregator) ever was:                                                                                                                                                                          |

- **Fedora — CONFIRMED, primary.** `https://mdapi.fedoraproject.org/rawhide/pkg/detox` → `version 3.0.1`, `release 7.fc45`, repo `release`, summary "Utility to replace problematic characters in file names". `https://src.fedoraproject.org/api/0/rpms/detox` → HTTP 200, package exists. The doc 04 line that called Fedora "referenced in passing... not independently confirmed" can be upgraded to confirmed on this evidence alone. **Fedora 38-44 specifically was not checked** — only Rawhide — so cite "Fedora (Rawhide verified; 38-44 unverified)", not the original version range.
- **Debian — CONFIRMED, primary, and the version range checks out.** `https://sources.debian.org/api/src/detox/` → `1.3.3-1+deb11u1` (bullseye/11), `1.4.5-4` (bookworm/12), `2.0.0-4` (trixie/13), `3.0.1-1` (forky/14 and sid/unstable), plus older 1.2.0 entries back to lenny. "Debian 11-14 + unstable" is correct as written.
- **Arch — CONFIRMED, primary.** `https://archlinux.org/packages/search/json/?name=detox` → one result, repo `extra`, `pkgver 3.0.1`, `pkgrel 1`.
- **nixpkgs — still `[SECONDARY]`.** Not re-verified from a primary source this pass. The "23.11-26.05/unstable" range rests on the same search summary as before; do not present it at the same strength as the three above. **New finding not in the doc**: local `brew info detox` shows the **Homebrew formula is deprecated** — "Deprecated because it is not maintained upstream! It will be disabled on 2027-07-28." Current bottled version is 3.0.1. This directly undercuts the doc's framing that detox's distribution footprint is a stable bar to match — Homebrew is actively winding it down on a fixed date, ~1 year from today. `[MEASURED]` + `[WEB]` `[stage-3]` The Homebrew half is confirmed from two sources. Local `brew info detox` prints verbatim: "Deprecated because it is not maintained upstream! It will be disabled on 2027-07-28." And the primary formula API, `https://formulae.brew.sh/api/formula/detox.json`, returns `deprecated: true`, `deprecation_date: 2026-07-28`, `deprecation_reason: "unmaintained"`, `disable_date: null`. Precision worth carrying downstream: **the deprecation took effect 2026-07-28 — three days ago — and the 2027-07-28 disable date is Homebrew's standard one-year projection shown by `brew info`, not yet a committed field in the formula data.** Analytics from the same command: 48 installs in 30 days, 816 in 365. Sources: local `brew info detox`, [formulae.brew.sh API](https://formulae.brew.sh/api/formula/detox.json). |
  | 8c | detox does not default to dry-run today (doc: "inferred... not independently confirmed against detox's actual man page") | **RESOLVED — CONFIRMED via local man page** | `[MEASURED]` Local `detox` binary (Homebrew, v-unstated by `--version`, which detox doesn't support) and `man detox`: `-n`/`--dry-run` is an **explicit opt-in flag** ("Doesn't actually change anything. This implies the -v option."). There is no dry-run-by-default behavior in detox. The doc's inference was correct; this should move from "inferred" to "confirmed." |
  | 9a | No trademark search performed; PyPI `detoxpy` exists as a naming collision | CONFIRMED | `[WEB]` PyPI API confirms `detoxpy` v0.1.5, summary "A tool to rename directories/files that contain unsafe characters" — a direct functional and naming collision in the same problem space, exactly as the doc flagged. https://pypi.org/pypi/detoxpy/json |
  | 9b | crates.io/GitHub name availability for any candidate successor name | UNVERIFIABLE AS WRITTEN | `[SOURCE]` The doc (correctly) proposes no candidate name, so there is nothing concrete to availability-check yet. One adjacent data point found: the crates.io name **`dist`** is squatted (placeholder, 2016, v0.0.0) — worth remembering if a future candidate name is a short, generic English word; short generic names are frequently pre-squatted on crates.io. |

## Corrections Required

Items are `C1`-`C6`, in their original order. **Stable — append, never renumber.**

1. **C1. f2's undo journal is not durable and is not XDG-anything.** It writes JSON to `os.TempDir()/f2/backups/<md5(cwd)>.json`. If Section 2's XDG recommendation is later used to imply "even f2 already does this," that implication is false — add an explicit note that f2's own undo storage is a cautionary example (temp-dir, hash-only filename, no state-dir usage) rather than a precedent to copy.
2. **C2. cargo-dist has been rebranded "dist"** since Oct 2024; the crates.io package name `cargo-dist` is still the one that actually installs it because the bare `dist` crate name is squatted by an unrelated 2016 placeholder. Update Section 5 to name both the old and current brand and note the crates.io naming snag, since a reader would otherwise land on stale branding.
3. **C3. figment is stale (last crates.io publish 2024-05-17)**, not "actively maintained" on par with `config-rs`. Either drop the "both actively maintained" framing in Section 4's table or explicitly flag figment as the higher-risk pick, noting the `figment2` community fork.
4. **C4. detox's Homebrew formula is deprecated with a fixed sunset date (2027-07-28)** — the doc's "detox packaged in all major distros" framing (Section 5, "the bar to match/beat") should note that this bar is actively eroding, not static. This is new information the doc entirely missed, not merely "unverified."
5. **C5. Terraform's stale-plan refusal is real but shouldn't be cited to the `apply`/`plan` doc pages** — those pages, read directly, don't state the drift-refusal behavior. Cite the actual "Saved plan is stale" error/mechanism (HashiCorp GitHub issues) instead, or find the specific docs page (if any) that documents it.
6. **C6. Fedora packaging of detox is now confirmed**, so Section 5/Confidence-notes' "Fedora...
   not independently confirmed" should be upgraded from unverified to confirmed — this is a case
   where the doc was too conservative, not too confident. `[stage-3]` **The basis for this item
   changed and got stronger.** It originally cited Repology, whose fetch had failed; it now rests
   on Fedora's own APIs (`mdapi.fedoraproject.org` returns detox 3.0.1-7.fc45 in Rawhide;
   `src.fedoraproject.org/api/0/rpms/detox` returns HTTP 200). Two limits on how far the upgrade
   goes: **Fedora 38-44 specifically was not verified, only Rawhide**, so the version range should
   not be restated as confirmed; and the **nixpkgs** entry in row 8b is still secondary-sourced.
   Debian (11-14 + unstable) and Arch (`extra`, 3.0.1-1) are now primary-confirmed. Doc 00 lines
   1424 and 1755 restate the distro list without the original "search-summarized" hedge, which for
   Debian/Fedora/Arch is now fine and for nixpkgs is still a live fidelity gap.

## Load-Bearing Uncertainties (still genuinely unresolved)

Items are numbered `U1`-`U5`. **Stable — append, never renumber.**

- **U1. `[stage-3, PARTLY CLOSED]` Repology is unreachable from this environment; the distro list
  no longer depends on it.** Original wording: **Direct Repology fetch still did not succeed cleanly in this session either** (WebFetch to `repology.org` failed/returned empty both times attempted); the Fedora/Debian/Arch/nixpkgs version list above rests on a WebSearch-summarized snapshot of Repology's page, not a direct primary-source fetch. Retry with a direct HTTP client before treating exact version numbers as final. `[stage-3]`
  **Retried, failed again — the fourth failure across three passes** (`curl
https://repology.org/api/v1/project/detox`, 45 s timeout, HTTP 000). Stop retrying it; treat
  Repology as unavailable from this environment. The dependency on it has been removed instead:
  Fedora, Debian, and Arch are now confirmed from each distribution's own API (row 8b, C6).
  **Residual, and it is narrower than before:** the **nixpkgs** version range (23.11-26.05 +
  unstable) and the **Fedora 38-44** version range are still search-summarized. Anything citing
  those two specifically is still `[SECONDARY]`.
- **U2. The claimed Debian debcargo/no-network-at-build burden was not re-verified live this round** — only cross-checked for internal consistency with well-known Debian Rust practice, not re-fetched from `rust-team.pages.debian.net` or the wiki.
- **U3. No trademark-database (USPTO TESS or equivalent) search was run** — this task explicitly said not to attempt legal conclusions, and none is offered. The `detoxpy` PyPI collision is a real, concrete data point but is not a substitute for a trademark search.
- **U4. axo.dev's blog post announcing the "dist" rename could not be fetched directly** (DNS failure on `blog.axo.dev` in this sandbox); the rename itself is corroborated by multiple independent secondary references (search results, GitHub docs/install pages using "dist" branding), so treat the rename as confirmed but the exact rationale/quotes from the original post as unverified.
- **U5. git clean's `clean.requireForce` default and f2's per-file abort-vs-best-effort behavior were not independently re-checked from source in this pass** — carried over from the original doc's own citations without new verification.

## Uncertainty-to-spike cross-check (doc 00 §11)

Checked in both directions at stage 3. Doc 00 was **not** edited; gaps are recorded here for the
propagation pass that owns it.

| This document's uncertainty                        | Doc 00 §11 spike | Status                                                                                                                                                                                                                                                                                                                                                                                                   |
| -------------------------------------------------- | ---------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| U1 Repology unreachable; distro list sourcing      | **Spike 10**     | Traces cleanly both ways, and **spike 10's job is now smaller**: Debian, Fedora (Rawhide), and Arch are primary-confirmed, so only the nixpkgs and Fedora 38-44 version ranges remain for it to settle. Doc 00 should narrow spike 10's scope.                                                                                                                                                           |
| U2 Debian debcargo burden not re-verified live     | **none**         | Reasonably not spiked — doc 00's own review record (line 1800) already hedges the citation explicitly instead of deferring it, which is the right call for a claim that only affects packaging effort estimates.                                                                                                                                                                                         |
| U3 no trademark search run                         | **Spike 1**      | Traces cleanly both ways.                                                                                                                                                                                                                                                                                                                                                                                |
| U4 axo.dev rename announcement unfetchable         | **none**         | Correctly not spiked — citation provenance, not a design fact in doubt. The rename is corroborated by multiple independent secondary references.                                                                                                                                                                                                                                                         |
| U5 `clean.requireForce` default; f2 per-file abort | **none**         | Correctly not spiked for `clean.requireForce`, which doc 04 uses only as an analogy. **But f2's per-file abort-vs-best-effort behavior is a different matter:** doc 00 designs detoxrs's own all-or-nothing vs. best-effort batch semantics, and no spike measures how the closest precedent actually behaves. Minor gap — cheap to close by reading f2's source, as row 1b already did for the journal. |

Reverse direction: spikes 1 and 10 are the only §11 spikes tracing to this document, and both
trace to real text (U3, U1). No spike cites a row or item here that does not exist. Doc 00 cites
this document 20 times and every citation resolves.

## Review record (stage 3)

| Finding (reviewer)                                                                                                                         | Verdict      | Action or reason                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| ------------------------------------------------------------------------------------------------------------------------------------------ | ------------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **CRITICAL** (L3): row 8b credits Repology as a verified primary source when the fetch actually failed; the real basis is a search summary | **MODIFIED** | The diagnosis is correct and accepted — the row claimed `[WEB]` strength for `[SECONDARY]` evidence, which is exactly the mislabelling doc 00 is most sensitive to. But L3's fix (relabel as secondary and move on) leaves a load-bearing packaging claim permanently weak. Repology was retried and **failed a fourth time**, so instead the claim was re-grounded in each distro's own API: Fedora (`mdapi.fedoraproject.org`, `src.fedoraproject.org`), Debian (`sources.debian.org`), Arch (`archlinux.org`). Net result is stronger than the original claim, with the two genuinely-still-secondary parts (nixpkgs range, Fedora 38-44 range) named as such. |
| **MAJOR** (L3): rows 8a and 1f say "CONFIRMED" while their evidence says "not re-verified"                                                 | **ACCEPTED** | Both changed to `CARRIED FORWARD`, and the vocabulary table now defines `CONFIRMED` as independently verified _in this document's pass_. L3 is right that one word for both makes an inherited citation look like a measurement.                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| **MINOR** (L3): row 7c's "CONFIRMED but incomplete" oversells a finding whose primary source was unreachable                               | **ACCEPTED** | Changed to `REFINED`, with the secondary-sourcing stated in the verdict cell rather than only in the evidence cell.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| **MAJOR** (L2): the evidence-precedence rule is incomplete; nothing arbitrates doc 05 against docs 10-13                                   | **ACCEPTED** | Judged sound and stated in full at the top. Its tier-3 reasoning is the load-bearing part and applies most directly to this document: docs 10-13 derive from detox's own source, so they have no jurisdiction over crate, packaging, or UX-convention claims. Added one thing L2 did not: **row 8c is a tier-1 claim** (it is about the C tool, not about prior art), so 10-13 outrank this document there. They agree, so nothing turns on it — but the rule should not be stated as if this document had no tier-1 rows.                                                                                                                                        |
| **MINOR** (L2): doc 00 lines 1424/1755 restate the distro list without this document's own "not a direct fetch" hedge                      | **MODIFIED** | Accepted as a real fidelity gap, but the fix belongs mostly here, not in doc 00: with Debian, Fedora, and Arch now primary-confirmed, the hedge doc 00 dropped is no longer needed for those three. Recorded in C6 and U1 that the hedge **is** still required for nixpkgs and for the Fedora 38-44 range.                                                                                                                                                                                                                                                                                                                                                        |
| **MAJOR** (L3): verdict vocabulary inconsistent across 05/06/07 and undefined                                                              | **ACCEPTED** | Shared table added to all three documents, plus `CARRIED FORWARD` for the inherited-citation case this document specifically needed.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| **MAJOR/CRITICAL** (L3): empirical results not consistently labelled measured vs. source vs. web                                           | **ACCEPTED** | Five-label scheme applied to every evidence cell. Most rows here are `[WEB]`; the three genuinely `[MEASURED]` ones (2a `man rg`, 8b `brew info`, 8c local `man detox`) are now visibly distinct, which matters because doc 00 weights measurement much more heavily.                                                                                                                                                                                                                                                                                                                                                                                             |
| **CRITICAL** (L3): Load-Bearing Uncertainties bullets unnumbered                                                                           | **ACCEPTED** | Numbered `U1`-`U5`; corrections numbered `C1`-`C6` in their original order.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| **MINOR** (L3): row 1g's "N/A — correctly excluded" is a fourth verdict category found nowhere else                                        | **REJECTED** | It is doing real work: it records that a suspicion raised against doc 04 was checked and found groundless, which is evidence about the review, not about ripgrep. Forcing it into `CONFIRMED` would assert a claim doc 04 never made; forcing it into `REFUTED` would assert doc 04 was wrong. Left as-is, and the same reasoning applies to row 9b's `UNVERIFIABLE AS WRITTEN`.                                                                                                                                                                                                                                                                                  |
| **MINOR** (L3): add the missing primary-source links (Debian Rust Team book, git docs, `python/cpython#95486`)                             | **ACCEPTED** | Added the Debian Rust Team book and Debian wiki URLs to row 8a, and `git clean` / `clean.requireForce` doc URLs to row 1f — both marked as linked-but-still-unread, so adding a URL does not silently upgrade an `[INHERITED]` verdict. `python/cpython#95486` belongs to doc 06 and was added there.                                                                                                                                                                                                                                                                                                                                                             |
| **MINOR** (L3): the Repology failure should be flagged as a must-re-verify-before-shipping item                                            | **MODIFIED** | Sharper action taken instead: it was retried, failed a fourth time, and the dependency was removed. An item that has failed four times across three passes should be recorded as unreachable, not queued for a fifth attempt. Spike 10's remaining scope is correspondingly narrowed.                                                                                                                                                                                                                                                                                                                                                                             |
| **MINOR** (L3): add verification timestamps to the Method section                                                                          | **ACCEPTED** | Stage-3 banner carries the date; row 7a already timestamps every crate version it cites, which is where staleness actually bites.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| (L1): no findings against this document — every version, date, and URL checked resolved and matched exactly                                | **ACCEPTED** | Recorded rather than acted on. L1 independently re-confirmed f2's `backup.go` path at tag v2.2.2, the `dist`/`cargo-dist` crates.io naming snag, figment's 2024-05-17 staleness date and `figment2`'s existence, and detox's non-dry-run default against a fresh build of `0a8e212`. No fabrication found.                                                                                                                                                                                                                                                                                                                                                        |
| Own finding, not raised by any reviewer: row 8b's Homebrew sunset date needed precision                                                    | **ADDED**    | `brew info` prints "disabled on 2027-07-28", which doc 00 line 1425 restates as a "hard disable date". The formula API shows `deprecation_date: 2026-07-28` with `disable_date: null` — the 2027 figure is Homebrew's standard one-year projection, not a committed field. The direction of the argument is unaffected (the footprint is eroding), but "hard date" is stronger than the data supports.                                                                                                                                                                                                                                                            |
| Own finding: U5's f2 per-file abort behavior is more load-bearing than it looks                                                            | **ADDED**    | Noted in the spike cross-check. Doc 00 designs detoxrs's all-or-nothing batch semantics, and the closest precedent's actual failure behavior has never been read, though row 1b shows reading f2's source is cheap.                                                                                                                                                                                                                                                                                                                                                                                                                                               |

**Staleness marking policy for this document:** annotate, never delete. Row 8b keeps a statement
of what it originally claimed and why that was wrong, because the mislabelled-source failure is
the finding — a silently-corrected row would leave no trace that a `[SECONDARY]` result had been
presented at `[WEB]` strength, and that pattern is the thing worth catching next time.
